define(['react'], function(React) {
  return React.createClass({
    render: function() {
      return (
        <select id="accounts">
          <option value="">---</option>
        </select>
      );
    }
  });
});
